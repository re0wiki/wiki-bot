# 鼠色猫语录全量迁移计划

2026-08-09 定。缘起与审计结论见 `docs/todo.md`「鼠色猫语录迁移质量修复」节；本文档是执行计划，优先级与状态以 todo.md 为准。

## 目标

`Module:鼠色猫语录` 数据子表 = **全量原档（日文）+ 中文（现有译文 + LLM 补译）+ 英文（英译回填）**；
全部完成后删除 `Category:存档` 存档页（删除是最后一步，前置是双向覆盖审计零缺失）。

## 决策记录（2026-08-09 用户拍板）

1. 未翻译内容**全量入库**，缺的中文用 LLM 补译（用户：现在 LLM 翻译质量已经不错）
2. ask 从 639 节选扩到**全量**（askfm 备份 2571 条）
3. narou 评论 560 条**建条目**（`鼠色猫语录/Web连载网站上评论` 空占位启用）
4. 十周年问答：**录音转写 + 英文对照**；能传 wiki 的原始资源尽量传
5. 英译字段（es/eq/ea）**尽量补全**（reddit 英译索引链接齐全）
6. LLM 选型：~~批量对齐/初翻用 DeepSeek Flash，疑难与抽审用 Kimi K3~~ → **2026-08-09 实测后改定 Kimi K3（kimi coding 端点）为主**：28 条对比中 K3 零实体/标题/译名错误、术语正确（ワイプ=划变转场）、行文自然（DS 需 2 处人工修 + 1 处归一兜底，K3 仅 1 处）；无注入版 K3 也认识主要角色（パック 不再译「包包」），但会在 **wiki 自有惯例**上失手（ベア子→贝蒂 ✗，应为贝亚子）且跨条目用字不一致（艾米莉亚/艾米莉娅/爱蜜莉雅碳混用）——译名表注入因此保留（一致性 + wiki 惯例，这类错误自动化兜不住）。K3 调用约定：OpenAI 格式 `https://api.kimi.com/coding/v1` + `User-Agent: claude-code/0.1.0`（无它 403，抄 Hermes Agent）+ **不并发**（算力不足易 429）；管线 provider 参数一键切换，DeepSeek Flash 留作备用。注意：译文审读与翻译同为 K3，存在共同盲区（セメント 俗语两个模型同错），关键条目仍以人工抽查为准

## 数据源

| # | 源 | 内容 | 形态 |
|---|---|---|---|
| S1 | wiki 存档页 31 页 | 中文译文（部分含日文/英文） | logs/archive_audit/archives.json 已快照 |
| S2 | `动画推特解说【全】rezeroneko.xlsx` | 实况解说日文推文全量 3044 推/79 天/2016-04~2021-03 | twint 结构化表 |
| S3 | `askfm 1.xlsx` | ask QA 日文全量 2571 条（2014-05~2015-10，含联动推链接） | 结构化表 |
| S3b | `askfm备用.docx` / `askfm 2xlsx.txt` | ask 同时段原始 dump / Q:A 文本 | 交叉验证用 |
| S4 | `Web连载网站上评论narou.xlsx` | なろう评论+作者回复 560 行 | 结构化表 |
| S5 | `2022_04_20_十周年问答.mp3` + witchculttranslation.com 英文总结 | 十周年 Space 问答 | 88MB 音频 + 英译 |
| S6 | reddit r/Re_Zero/wiki/translation | 英译索引（QA 全系列 + 实况 S1 13-25/新编 3-13+OVA/S2 全） | Wayback 快照 logs/archive_audit/reddit_translation_index.html |
| S7 | 原推 56 条 | 生日问答等原推（fxtwitter 可机读；问答 thread 抓取方案待定） | 在线 |
| S8 | 现有模块 27 子表 | 含存档外扩充（菲莉丝 46% 等）——**不可丢弃** | logs/archive_audit/modules.json 已快照 |
| S9 | 签名会日文 Collapse | **孤本**（privatter 已死无快照） | 在 S1 存档页内 |

原档备份目录：`C:\Users\ccxxx\Desktop\长月qa原档备份`（用户本地工作副本，不入 git；归档副本在 OneDrive `文档\杂项\re0\长月qa原档备份`）。

## 技术约束与风险

- **性能约束的实测结论（2026-08-09，4 倍规模模拟）**：Lua 侧远未触限（4x 数据 require+扫描 CPU 仅 0.4s，Scribunto 限 10s）；`frame:preprocess` 贡献仅 ~10% 且**不可去**（无 preprocess 时 `<ref>` 变字面文本、`{{Seirei}}` 等模板不展开）。**真正的硬约束是 MediaWiki post-expand include size 2MB 上限**（与页面源码 2048KB 上限同为 2MiB，对同一数据量级触发）：当前全量输出 1.1MB，4 倍后 ~4.4MB → 输出被整体丢弃（仅剩警告链接）。对策已定（2026-08-09 用户裁决）：`table=` 参数已上线（按名懒加载单表，向后兼容逐字节验证），`/all` 页届时拆为**动态子页**（每页 1-3 次 `query|table=` 调用，各自独立 2MB 预算），不引入 bot 编译层——静态编译不推高容量墙（编译产物同样撞 2048KB 源码限），且语录数据改动不频繁、一次性检查即可，无需常驻巡检。**修正（2026-08-12 P3 实测）：post-expand 是整页聚合，不是每次调用独立预算**——单页放两个 `query|table=` 调用合计 2.9MB 仍被整体丢弃；拆页而非拆段才是解法（/all/早期ask 已拆两个分表子页 + 索引页，见 P3 行）。拆分粒度按表/按季等有逻辑的单位（如实况解说按季拆子表）。**/all 拆页已实施（2026-08-10，P2 上线后全量 3.49MB 撞墙）**：6 个动态分表页（角色 872KB / 早期ask 401KB / 实况旧版 771KB / 新编+OVA 477KB / 第二季 723KB / 其他 80KB，均为渲染后 HTML 实测），/all 改索引；分组留了 P3/P4 余量（早期ask 独占页——全量后 ~1.6MB；角色页 P6 英译回填后 ~1.1MB）；空占位表（帕克/福尔图娜/Web评论）不上分表页（避免空结果提示与分类污染），有内容后加回。当日曾短暂上线静态编译试点（re0_quotes_all + jobs quotes-all + /all 静态化）并已整体撤销
- **Fandom 上传**：允许 mp3 ✓、**xlsx ✗**；文件大小限制待实测（88MB mp3 可能超限，备选 archive.org 托管 + wiki 外链）；xlsx 类原档归宿已定（2026-08-09 用户手动归档至 OneDrive `文档\杂项\re0\长月qa原档备份`，re0-corpus 与 wiki 均不适合 xlsx）
- **译名一致性**：LLM prompt **注入 ja→zh 专有名词表**（2026-08-09 用户裁决，反转当天早些时候的「不注入」决定）：目的不是规定用字，而是让弱模型**识别专有名词**、不把角色名当普通名词（试点 v1 曾把 パック 译成「包包」）。表由 `logs/p0_build_name_table.py` 从 zh 站角色页 infobox（name/name_ja_kanji/name_ja_kana，跟随重定向，短名取首段）抓取 + 手工补充段（术语/昵称/俗语）生成 → `logs/name_table_ja2zh.json`（90 条）；表只收 Re:Zero 作品内专有名词与相关人物——**一般世界知识不收**（かぐや様=辉夜大小姐、ドラえもん、鳳凰院凶真 等其他作品要素靠译名表补不全，2026-08-09 用户裁决）；infobox name 字段偏全名且个别繁体，短名以译名表标准为准（菲莉丝/艾尔莎/罗姆爷）。昵称惯例按 infobox 严格区分：**ベア子=贝亚子、ベティ=贝蒂**（试点曾错误归一，已回改）。本地归一（translation fix 规则）仍保留作兜底（注入后 v2 仍漏了 1 处 贝阿特丽丝被兜底抓住）；`fix:translation` 的 generator 不覆盖 Module 命名空间是**有意为之**，确证时用 `-simulate` 手动指定子表。**若注入后质量仍不达标就换模型，不让另一模型全量检查**（かぐや様 类低频词处理不好时全检失去意义）
- **abbr 机制已废除（2026-08-11 用户裁决）**：语录子表的 `src` 键经 `p.abbr` 查表展开改为全量内联（未来要补逐条准确推文链接，缩写层没有存在价值）；主模块查表逻辑同步删除（`local src = data.src`），30 表渲染与基线逐字节一致（注意 parse 输出尾部的 NewPP/Transclusion 注释含计时信息，对比需先剥离）；菲莉丝表有派生 abbr 行（`p.abbr["2018-01-16 N"] = p.abbr["2018-01-16"] .. 后缀`）需先求值再展开；管线生成器（logs/quotes_lua.py）不再产出 abbr 块。同日续：abbr 展开带进来的 **`[[模块:鼠色猫语录/X|文字]]` 自指链接全量移除**（2130 处，用户裁决：那是技术实现不是来源），src 来源文字改为纯文本（logs/p2_remove_selflinks.py）
- **对齐留痕**：zh↔ja 对齐（实况 66 集、ask 639 条）全部产出映射表存 logs/，按比例人工抽查，不许静默通过；**映射表必须带内容断言**（关键词 ↔ 推文序号，OVA 试点曾错位一格被抓出）
- **LLM 译文必经人工抽审**；人工修正在构建脚本 MANUAL_FIXES/BULLET_FIXES 留痕。试点实测：v1（无注入）28 条抓出 3 处必修（パック→「包包」实体误译等）；**v2（注入译名表）实体误译清零**，仅剩 2 处人工修正（メモリースノー 标题被意译、セメント 俗语误译「水泥」）+ 1 处归一兜底（贝阿特丽丝）
- **管线约定**（OVA 试点定型）：推文入库剥离 `  #rezeroneko` 尾签（原推链接保留全量）；译名归一在本地构建后跑（`logs/p0_ova_normalize.py` 直接应用 user-fixes 规则），上传后 `-fix:translation -simulate` 确证零改动；OpenCode 该模型偶发把答案放进 `reasoning_content` 以 `</final>` 分隔、content 留空，客户端已兜底
- 入库统一简体；模块字段值是 wikitext（`:wikitext()` 渲染），`{{Seirei}}` 等模板可用，字面 `}}` 是 bug 要杜绝（旧迁移教训）
- reddit 等被反爬源：用户已连接本地浏览器可实时抓取；Wayback 快照偏旧作兜底（快照已验证可用）。tieba 译文帖经 browser 工具实测可读（2026-08-09：p/4621079261、p/6423124322 全文+回复完整；偶发「百度安全验证」由用户手动清除后重试；p/4731166958 首访撞验证，真实状态待复查）——但百度有吞帖/吞回复史，优先级低

## 阶段计划

| 阶段 | 内容 | 产出 | 估时 | 依赖 |
|---|---|---|---|---|
| **P0 基础设施与试点** ✅ 2026-08-09 完成 | LLM 接入脚本（`logs/quotes_llm.py`：OpenCode Zen + 429 退避 + `</final>` 兜底）；Lua 生成器（`logs/quotes_lua.py`：转义 + 花括号配平校验）；性能分解实测与架构方案（主模块重写 + table= 已完成，/all 拆法待 P2）；**OVA 试点全流程走通**：44 推文 + 1 存档独有 = 45 条目上线（`Module:鼠色猫语录/动画实况解说/OVA`），对齐映射/译文/构建产物存 logs/archive_audit/ova_*.json|lua | 管线脚本 + OVA 上线 | 实际 1 会话 | OpenCode key（已实测可用） |
| **P1 无损小修** ✅ 2026-08-09 完成 | F（重复键 ×2）、C（截断 8 处）、E（零散 2 条）、D（奥托 2017 9 组）、B（签名会 js 孤本 159 行）——已上线并通过复跑审计与渲染抽查（logs/p1_apply.py） | 直接修模块 | 实际 0.5 会话 | 无 |
| **P2 动画实况解说** ✅ 2026-08-10 完成 | 64 集映射（深夜档偏移/零散日人工核定）；58 集 k3 对齐（zh 全覆盖，映射抽查正确）；补译 1534 条（机审 1534 中仅 1 误报——译名覆盖检查法规模化验证通过）；132 条孤儿定性为存档编者注/解说（带 ref 标注入库）；3 子表上线（最终）：旧版 1419 / 新编 702 / 第二季 1012 条（+OVA 45）。**存档 4 页内容已全部入库**。**1:1 规则（2026-08-10 用户裁决）**：条目与推文一一对应——存档译文**仅在 1 译文 ↔ 1 推严格对应时采用**；合并（多推一译，93 组）与拆分（一推多译，21 组）均难以无损对应，涉及的推文逐条以 k3 重译（共弃用 141 条存档合并/拆分译文、留痕计数）；**弃用译文中的译者加注不单独保留**（避免重复信息），其中 19 处并入对应推文条目的译文（`<ref>` 原文提取或括注，如路人=奥托、パトラッシュ 命名出处、「妈妈」伏笔等），1 处无对应推文的编者注块（第二季 36 集 Z14「妈妈」分析——对齐实为编者注，xlsx 无对应推文）保持独立编者注条目。编者注来源按存档页署名精确到人：第二季/新编=零の幻影，旧版按集（默认出禁豚@Re0主吧，8、9 集=サテラ@S1，22 集=Leonopteryx（幽邪）@巴哈姆特，1 集=QA君）。**条目级译者署名已上线（2026-08-11）**：src 行追加「· 译：XXX」——存档译文署人类译者（同上映射），k3 译文署「Kimi K3」；实测每条 +22B HTML，旧版页 771→815KB（最长寿面仍远低于 2MB）。**推文附图与引用（2026-08-11 补充）**：xlsx 全量仅 2 条带图推 + 1 条引用推——2 图已传 wiki（File:动画实况解说-罗兹瓦尔生日花束.jpg / File:动画实况解说-昴的手机实机.jpg，经 vision 核实内容）并**嵌入条目正文**（惯例：Nico 表已有 `[[File:...|400px]]` 内嵌先例，不进 src）；引用推（大塚真一郎贺图推）链接入 src。**发推时间已入 src（2026-08-11 用户裁决）**：JST + 30 小时制（深夜番惯例钟点，如 `2016-04-03 25:30 JST 第1集`——日期与官方播出日/存档节日期对齐，播前推 24:xx 自然归在播出当晚）；管线从 xlsx CST 逐推转换（+1h，JST<06:00 则日期-1、小时+24）；**排序键同步改用 JST 实际时刻**——修掉了按 CST 时间字符串排序把播前推（前日 23:xx）挤到正片场（00:xx）之后的隐藏顺序 bug；3 个实况分表页头部加了口径说明。**旧版存档编号残留清理（2026-08-11 用户发现）**：旧版存档按集编号条目（1. 2. 3.）是存档自身流水号、非内容，187 条 1:1/编者注条目的条首编号与 `<poem>` 标签已在 clean_zh 统一剥离（含「[tieba 原帖]」前缀后换行接编号的情形——br() 转换在 clean 之后，匹配须兼容 \n；后随数字如 2.5 不剥）；新编/第二季/OVA 无此问题（bullet 制无编号）。**角色表/ask 的署名（2026-08-11 普查+统一）**：大部分本就在 abbr 里随 src 可见（零の幻影/Kasakunko/soledad粉译+零の幻影校/琬/haneko/Haneko/雨宮優子/墨海/星空宅梦/喵醬等），已统一为「 · 译：X · 校：Y」末尾格式（18 表 26 处，logs/p2_unify_credits.py；正则曾把「英译」误判为署名「英」，加前导断言+词尾黑名单修复）；**存档页与 abbr 均无记录的保持不署名**（用户裁决：首创编辑者未必是原创译者，不猜测）——涉及佩特拉2018/奥托17·18/加菲尔/威尔海姆/罗兹瓦尔/Nico/签名会/爱蜜莉雅ask/菲莉丝2018 等。坑：Fandom 垃圾信息过滤器禁 t.co 链接（构建时按 twint urls 列换成展开目标）；旧版存档是空行分段非 bullet；旧译 486/emt 归一；k3 会复述推文内 t.co（译文也要过 sanitize） | `鼠色猫语录/动画实况解说/*` 全量 | 实际 1 会话 | P0 |
| **P3 ask 全量** ✅ 2026-08-12 完成 | 对齐 614/639：3 条带 jq/ja 锚点 + 窗口法 76（前段时间序）+ 分块 q-only 546（**后段是主题分组非时间序，窗口法命中崩塌后改全表分块**）+ K3 答案兼容性校验层（q-only 对重复/相似问题会误配——撤下 8 条误配、13 条跨块冲突中 4 条换到第二候选正解，含人工抽查发现的 Z129/Z295）；25 条孤儿（8 撤下 + 17 未命中）带「xlsx 无对应」ref 入库。署名：存档页两条分隔线后 635 条 = Kasakunko（用户按贴吧原帖 p/7160371226 确认，浏览器核对开头逐条一致），两条线之间追加段 3 条 + 手动补录 1 条无记录不署名。补译 2629 条（K3）：**机审抓出 117 条 ja 有答案但 zh 答案为空**（批处理静默漏译，加断言后全部重译）；进一步发现 **836 条 xlsx 推文本身被 Twitter 字数截断**（尾部「…」）——从 docx 主页存档回填完整原文 794 条（42 条 docx 也无，留痕），其中已按截断文翻译的 524 条全部用完整原文重译；管线统一优先 ja_full/jq_full 字段。docx 交叉验证：初判 674 条独有 → **模糊判重（Q 相似 ≥0.60 且 A ≥0.40 双指标）剔出 504 条实为 xlsx 重复**（长月转发时改写过措辞 + 截断，精确/前缀匹配漏判；D84 类不同问相似句在 0.57 被正确排除）→ 真独有 170 条带来源 ref 排表尾；另从 docx 修复 2 条截断答案（no=1887/1987）。归一 232 处（雷古勒斯→雷格鲁斯、培提其乌斯→培提奇乌斯等）；多行 Q/A 内部换行统一转 `<br/>`。部署：单表渲染 ~2.2MB 撞 post-expand 墙 → **拆两表按量均衡**（早期ask/2014-05~2014-08 计 1650 条 / 早期ask/2014-09~2015-10 计 1114 条含补录——按日历年拆则 2014 表 P6 英译回填后仍超墙，故不按年）；分表页渲染 1567/1337 KB、条数精确、探针全过（Kasakunko/Kimi K3 署名、docx 补录注、孤儿注、首末日期）；主模块 data_names 注册、/all/早期ask 改索引页、旧单表删除、/doc 更新（31 子表）、全站无旧表名引用。产物 logs/p3/（alignment.json、translations.json、docx_extra_dedup.json、conflicts.json、逐批断点）。**K3 稳定性坑**：504/500 频发需重试；配对任务 reasoning 极长会烧穿 max_tokens=16000（残余 563 条分四批调用才稳）；偶发批次漏出某条（D 前缀编号回传裸数字需兼容）；答案校验批 max_tokens=2000 不够（reasoning 1997/2000），8000 稳。**管线顺序坑**：本地归一必须在最终构建之后跑——拆表重建曾冲掉已归一的 232 处（部署后靠 grep 探针抓出补部署）；期间发现 K3 惯用转写「雷古勒斯」是 p2o 覆盖不了字序调换的变体，已作为别名入 translation_manual | `鼠色猫语录/早期ask/*` 全量 2764 条 | 实际 1 会话 | P0 |
| **P4 narou 评论** ✅ 2026-08-12 完成 | 勘察定规格（2026-08-12 用户确认）：560/560 全有作者回复（中位 96 字符，≤10 字符纯确认仅 19 条，实质内容为主）；**笔名收进 src 不带 mypage 链接**（`2020-11-05 なろう感想（MoNo）`）；**三栏合并带标注**（【好评】【指摘】【一言】，单栏一言不标注——占 60%）；**不补章节锚点**（chapter id 仅 4/560 有值，按时间倒推有误标风险；评论文本常自带章节引用）；page 字段（评论列表分页号）不收。K3 补译 560 条零空答案；机审 7 处译名告警全是误报（ハーレム/チーレム/スラムダンク 子串 + 指摘引用原文保留日文）；归一 83 处。启用占位表 Web连载网站上评论（887KB），/all/其他 加段（1116KB 渲染正常），子表与主模块 /doc 同步。产物 logs/p4/（narou.json、translations.json） | `鼠色猫语录/Web连载网站上评论` 560 条 | 实际 0.5 会话 | P0 |
| **P5 十周年问答** ✅ 2026-08-12 完成（主线） | **转写选型实测 14 个模型**：本地 whisper large-v3（RTX 4090，faster-whisper，122 分钟 7 分钟跑完）胜出；OpenRouter 云侧 MAI-1.5 最优（$0.03/5min）；其余出局——LLM 系转写（gpt-transcribe/gpt-4o/voxtral/fish/qwen3）共性病灶是把冷僻专名「规范化」成常见词（オットー→夫/おっと、リゼロ→ディフェロ），chirp-3 需 `ja-JP` 完整语言码且有 ~1 分钟同步上限，parakeet 无日语，whisper 托管系与 gpt-4o-mini 先验排除。**双引擎整合（2026-08-12 用户拍板双全量）**：whisper（时间戳骨架+置信度）× MAI（高保真）→ difflib 字符级对齐（一致率 87-95%）→ 分歧确定性分类（填充词/微 replace/假名变体自动裁决 1938）→ 968 处真分歧 K3 仲裁；**MAI 5 分钟硬切的边界重复碎片**（24 边界）由 K3 语义去重裁 21 处。**QA 抽取**：K3 按「次、XXさん」结构抽 106 条——**13 个段整段误出中文，根因是中文 prompt 对输出语言的 prime，改全日文 prompt 后重跑清零（用户提议）**；去重后 103 条。**英文对照**：witchculttranslation 总结解析 102 条（Q103 无 handle 变体、Q79 系英译者跳号），K3 配对 102/102 全覆盖（骑士问为 ja 独有、FMI 重复剔除、E16 错配修正）；90 条带提问原推链接入 src；72 条短语精确定位时间戳（其余 chunk 粒度诚实标注——whisper 会跳读寒暄，匹配上寒暄撞车是假精确，宁粗勿错）。翻译 103 条 K3（中文 prompt）+ 归一 18 处；新表 十周年问答 上线（32 子表），/all/其他 1428KB 渲染正常。**K3 调用策略修正（用户点拨）**：reasoning 模型 max_tokens 按实际生成计费，低上限烧穿=推理费白付+重试再付——kimi 调用恒用 32768 + 超时≥1800s，不再逐档爬升。mp3 归宿（2026-08-12 用户拍板切开传）：88MB 超 Fandom ~10MB 上传限 → ffmpeg 流复制切 13 段 10 分钟文件（无损，7.2MB×12+1.6MB）传 File 空间（File:十周年问答录音 part01-13.mp3），条目 src 时间戳链到对应分段——音频 File 直链会渲染成白色播放器组件（视觉太重且外观无参数可调，2026-08-14 用户定），故用前导冒号纯文本链接（`[[:File:…|录音约 M:SS 处]]`） | `鼠色猫语录/十周年问答` 103 条 | 实际 1 会话 | P0 |
| **P6 QA 重核** ✅ 2026-08-14 完成（英译回填已取消） | **2026-08-14 用户裁决：不再回填 eq/ea/es**——英译字段的原始价值（部分 zh 曾从英文转译的来源链、三方校对）已分别被 src 链接与 AI 现译覆盖，存储英文只剩 ~30% HTML 成本；既有英文字段（角色表 eq/ea、十周年问答英文梗概）暂留，**待日文一手来源收集完毕后统一清理**（届时同步撤下 十周年问答 的 eq/ea）。取消后各表体积压力解除（早期ask 按量拆的两表无需再担心 +30% 超墙）。**原推获取通道**：存档页/reddit 英译帖只链预告推，EN wiki 角色页 Trivia 直接链回答推（102 条，fxtwitter 抓到 88 + 提问推 40）+ **wayback CDX `twitter.com/nezumiironyanko/status/*` 47794 存档推页**（雪花 ID 解日期免请求；2021 年有人整抓过时间线）。**回答滞后提问**：实证佩特拉 2019 生日（02-14 预告）的回答发在 03-10——按日池化全灭，须「场次日 +45 天窗口池」；且**作答日可能整天零存档**（蜜蜜 05-06、弗雷德莉卡 01-02/03 等，fxtwitter 需已知 id、时间线枚举要 auth API，这些缺口当前通道补不齐）。**匹配**：K3 zh 条目×日文推池配对 **629 组**（抽查 17 对全对）回填 ja（+jq 36）；两场次零覆盖：奥托 2017、爱蜜莉雅 2017（2017 存档稀疏）。**重核（本阶段核心价值）**：689 对既有中译×日文原文逐条核对，**92 条实质错译（13.4%）**——当年经英文转译的老译文存在事实颠倒/对象错误/虚构（如奥托睡草坪段被编成童话故事）；用户裁决 K3 自 ja 重译覆盖、署名改 Kimi K3（旧译留 wiki 历史）。**事故留痕**：按 zh 文本定位条目遇重复会双写（艾姬多娜「雷德对话」与「欧米伽权能」两条同 zh——迁移期既有 bug，修正被误写到两条，已手工修复 zh+原推链接）——修正类操作必须按块定位不按文本。解析坑：语录子表三种手写格式（双引号空格/单引号/制表符无空格），字段正则必须 `\s*` + 单双引号兼容否则静默解析为空。**原推链接入 src（2026-08-14 用户指示）**：629 组配对的原推链接写入 src——新增 204、**替换 424**（迁移期 src 链的「原推」大多是场次**预告推**，非逐条回答推，罗姆爷全表 11 条曾同链一条预告推）、已正确 1；抽查 201 条链接 id 与 ja 字段同源一致、渲染正常（logs/p6_srclink.py）。**后续清理（同日用户抓出双链接）**：链接检测正则漏了 `mobile.twitter.com` host 变体导致 164 条新链接与残留手机版预告推链接并存 → 删除多余旧链接（logs/p6_clean_links.py）；进一步把**未配对条目**上 mislabel 的场次预告/募集推链接（正文逐条分类确认：誕生日/質問募集关键词，24+4 个推 id）标签「原推」→「预告推」共 573 处（logs/p6_relabel.py）；真内容推（回答推/事实推）保持原推标签不动。产物 logs/p6_*.py|json（cdx 池/时代池文本/匹配/重核/错译清单 p6_issues.json） | 19 角色表 ja/jq 回填 + 92 条重译 | 实际 1 会话 | P2/P3 后效率更高 |
| **P7 收尾** ✅ 2026-08-15 完成 | 覆盖审计对月表复跑（dump/analyze 脚本改数据源；配对取消一律查并集）：874 缺失行 K3 三分类——722 b 类（旧译文已被 P2/P6 新译覆盖，留痕接受）+ 83 a 类（样板/署名/元信息）+ **69 c 类有独立信息量编者注全量保留**（68 条按上下文归月入 lua_base 对应月表，src 标「编者注（迁移自存档页，原属章节：X）」；签名会 1 条已在主页面不重复）。链入改指 6 处（Wiki-navigation/术语：异世界文字/角色：夏乌拉/菲鲁特/莱茵哈鲁特/月表内链）+ QA 模板族 4 页（零转置孤儿）+ Tab/Comment 删除；**存档 31 页 + Category：存档 删除**；`存档:` 伪前缀退役（user-fixes.py 与 Module:Title 双注册点同步）；Wiki-navigation 重编译；pytest 75 过。re0_nav 调用约定：`-page:"MediaWiki:Wiki-navigation"` | 存档页删除 | 实际 0.5 会话 | 全部 |

P1 与 P0 可并行；P3/P4/P5 相互独立，P0 完成后可任意顺序。

## P8 全量推文时间线 + 统一时间分表（2026-08-14 用户提议，计划中）

**缘起**：P6 建池时发现 wayback CDX 有长月 19,042 条唯一推文（47,794 快照行去重），其中 2020-2026 约 1.6 万条从未对照 wiki 评估——含 **S3 实况解说**（2024-10~2025-04 播出窗口 4,603 条，wiki 实况表只到 S2）与近年生日问答。用户裁决方向：**机械全量收录**，按时间分表，中文翻译暂缓，放弃筛选/归组以压工作量。

**关键勘察结论（2026-08-14，logs/p8_survey*.py）**：主题轴是**查询层**实现而非物理分表——`{{Q|关键词}}` 全文检索所有注册表（210 个 `/猫语` 消费页全是关键词调用，如 `{{Q|12卷}}`）；require 数据表不占 post-expand（P0 实测 4x 数据扫描 CPU 0.4s/10s），Q 只输出匹配条目。→ **物理分表改为纯时间轴对现有消费方零破坏**，一套分表逻辑可行。

**既有数据时间精度审计（P8-A，2026-08-14 完成，logs/p8_audit*.py）**：~8,120 条中 **96.5% 月级精确**（雪花链接 ~5,400 含 ask 表 2,568 条联动推、ISO/JST ~2,400）。不精确 288 条处置：**ask 孤儿 195 条（真·无日期）放 `2014-09` 月表、src 标注「日期不明」（用户裁决）**；罗兹瓦尔/爱蜜莉雅表内 ask 抄录 ~76 条与由里乌斯实况抄录 1 条经文本比对从 ask/实况表继承精确日期（顺手完成部分内容级去重）；爱蜜莉雅 2017 生日会 9 条语义定为 2017-09；罗兹瓦尔 8 条无日期另查。

**方案**（决策点 2026-08-14 用户已全部拍板：**全源合并**统一时间分表；**不加 tag 字段**、主题检索仅按关键词；**已删除推文全收**；**2013-2016 早期推全收**；**id 级机械去重构建期直接做**（既有表 src 链接提取推 id 集合，导入时剔除），挂待办的仅 **LLM 内容级去重**——既有条目未链推 id 但内容与推重复的情形，与翻译同级；**分表粒度定案：月度**——峰值月 2026-04 973 条按 1KB/条高估 ≈1.0MB 仅占 2MB 墙一半，季度 2.4MB 已超；远期补译后峰值月或贴墙（~1.9MB 高估上限），届时最肥的 S3 期月份再切半月表）：
- 表结构：`鼠色猫语录/YYYY-MM` 月度表（2013~2026 约 150 个；峰值月 ~650 条 ≈ 400-600KB 渲染，离 2MB 墙远）
- 条目字段：推文 = `{ src = "YYYY-MM-DD [twitter 链接 原推]", jq = 提问推, ja = 推文原文 }`（jq 经 fxtwitter `replying_to` 链反查，已实测成功率 82%，2024+ 更高；抓不到的留空）；zh 全留空待补
- 既有数据并入：解析现存 34 张主题表（P6 三格式兼容正则已解决）→ 按时间戳重排进月表。时间戳来源：推文条目走雪花 ID（秒级）、ask/narou/十周年走 src 日期、实况走 JST 30h 时戳
- /all 改造：按年分子页（肥年如 2025=4,749 条须再切季度/月，单年整页会超墙）
- 主模块：`data_names` 注册改为时间序生成

**阶段**：
| 步骤 | 内容 | 预估 |
|---|---|---|
| A 勘察 | 消费方调查 ✅（本节）；现存条目时间戳可得性审计（无日期条目的兜底） | 0.5h |
| B 抓取 | 19k 推正文（fxtwitter 活推 + wayback og:description 兜底）+ 提问推反查；断点续跑 | 后台 3-7h |
| C 构建 | 解析现存表 → 条目池；合流新推（**id 级去重**）；月表 emit；**round-trip 校验**（解析重出与现表逐条比对防字段丢失） | 1-2h |
| D 部署 | 新月表 + data_names + /all 年度子页 + 旧主题表删除（校验全过后） | 1h |
| E 暂缓（待办） | **LLM 内容级去重**（未链 id 但内容重复的既有条目）；**中文翻译**（月表上增量补）；2026+ 新推持续收录的自动化（另议） | — |

**风险**：fxtwitter 对万级请求可能限流（断点续跑对冲）；round-trip 解析保真是删除旧表前的硬门槛；月表数量多（~150）但均为机械生成/注册。

**切流（2026-08-14 完成）**：用户裁决——不建公共分表页，全量列表放各月表 /doc 子页；/all 系列 9 页重定向到主页面 `鼠色猫语录`（说明同步改写）；`Template:Q` invoke 目标切至 NekoQuote（210 /猫语 页抽查渲染正常）；建 `Category:模块:NekoQuote 查询结果为空`；旧族 68 页删除（32 数据表 + 主模块 + /docs + Tab/Author + 旧空结果分类；删除前核实直接引用仅 2 处 `{{Q}}` 转调——pywikibot `Page.delete()` 默认交互确认，非 TTY 下静默不删，须 `prompt=False`）。切流后验证：新旧模块 4 组关键词结果集一致（ref 脚注编号差异规范化后），猫语页/月表页渲染正常。**导航架构（同日分层演进）**：初版 162 个月签平铺视觉过重 → 仿 Tab/Anime_S1 两行分层 → 年签子模板重复代码过多 → 定稿 **`Module:Tab/NekoQuote` 标题驱动**（页标题解析年月，年签行恒显 + 当前年加粗 + 本年月签行；复用 `Module:Tab._tab`；Template 仅一行无参 invoke）；配套建 113+10 张空月表使 2010-01~2026-12 全连续（共 204 张），导航永无红链、数据合流零维护。

**合流（2026-08-15 完成主体）**：抓取两阶段管线（fx 快线 4 线程 6.8/s + wb 慢线多快照遍历；断点续跑防 Hermes 重启/中断）。**作者过滤意外立功**：CDX 19,042 唯一 id 中 ~10k 实为他人推被归档在 nezumiironyanko/status/ 路径下（fxtwitter 按 id 解析出真实作者，如 Rezero_official 981 条）——他本人在库推 ~8.8k。合流 **4,135 条新推**（id 去重既有 6,244），全库 12,232 条/162 张有内容月表；round-trip 校验既有 8,097 零缺失。**Fandom 垃圾过滤器**：t.co/a.co 短链剥除、youtu.be 机械展开完整 URL（封禁ID #855849 三连拦后全过）。**遗留**：2,021 条已删推核销（2026-08-15 用户定）——fxtwitter + Twitter 官方 oembed 双通道 404 实证为作者删除（长月播后删实况推是惯例，同 S1）；wayback 重试 900+/2021 零捞回（2019 后捕获全是 JS 空壳），archive.today 用户自查亦无。其中 S3 实况期 ~1,500 条的价值由**零の幻影 B 站专栏日译中**承接（爬取另列待办，暂不执行）；其余不再投入。**同日转机：wiki Discord 的推特转发频道（2024-10~2025-10 运行，已停）经 bot token 全量导出 3,663 条消息（logs/p8_discord_fetch.py）——转发消息 content/embed 里带推文全文快照（Discord 服务端存储，推删不影响），533 条已删推回填月表（8 张变更已部署）**，余 ~1,490 条（多为频道停转后的 2026 与频道开播前的 2020-2022）核销。提问推 273 条入 jq（近年推以实况连载为主、@问答少）；构建器数据源已切本地基线 `logs/p8/lua_base`（旧主题表删后 wiki 唯一副本即月表），部署增量比对快照 `logs/p8/lua_live`。

## 验收标准

- 双向覆盖审计（logs/analyze_archive_coverage.py + reverse_coverage.py）零缺失
- 每批 LLM 产物（对齐/翻译）有映射表 + 抽查记录
- 渲染验证：修改子表前后 parse 对比（注意 PortableInfobox/parse 缓存坑，见 modules.md）
- 性能：全量架构下关键词查询 < 10s CPU；~~/all 按既定方案（动态子页）完成拆分~~（已于 2026-08-10 拆为 6 个动态分表页）

## 用户侧待提供

- [x] LLM API：~~OpenCode Zen `deepseek-v4-flash-free`~~ → **Kimi K3**（2026-08-09 实测选定，见决策记录 6）。key 由用户持有，**不入 git 不入 memory**；管线脚本读 `OPENCODE_API_KEY` 环境变量，或 gitignored 的 `logs/api_keys.json`（opencode/kimi 两个 provider 配置）
- [ ] 录音转写偏好：本机有 RTX 4090，可本地 faster-whisper；质量待 P5 实测，不行再走 Whisper API（约 $0.5）
- [x] 浏览器：用户已连接本地浏览器（reddit 等被反爬源可改走实时抓取；Wayback 快照偏旧，作兜底）
