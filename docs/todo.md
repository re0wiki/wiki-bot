# wiki-bot 跨任务待办与待决策项

新增规则：已完成的待办直接删除（信息并入对应领域文档），本节只留未完成项；有多事项任务时按「目标/已完成/当前阻塞/下一步/外部依赖」结构化。

## 待处理

### 鼠色猫语录（P0–P8 全部完成，2026-08-09 ~ 08-15）

**全程记录：`docs/quotes-migration.md`**。语录库现为 `Module:NekoQuote` 月表架构（13,905 条 / 170 张有内容月表），全条目中文可读。

- [ ] **LLM 内容级去重**（P8 衍生，已按实证缩小范围）：ja-ja 模糊匹配（744 条无 id 带日文的既有条目 × raw 推；归一化精确/包含命中下界 26 条）+ 既有表内部去重（同内容不同措辞，如实况抄录 vs 实况表）。439 条纯中文无 id 条目是 ask.fm 源，非推文，排除。估计 ~0.5 会话
- [ ] **2026+ 新推持续收录**（方案已定稿，周期执行）：EN Discord 转发频道 Discrub 定期手动导出 → `logs/p8_discord_merge.py <导出目录>` 一键全链。**runbook：`docs/nekoquote-incremental.md`**。建议频率：每月或有新内容时

## 已决策/已评估决定不做（勿再提）

- **2026-08-15 S3 实况零の幻影 B 站专栏不抓取**：内容面四通道（EN/中文 Discord 转发 + wayback + fx）已并集完备（EN 频道 S3 窗口本人正文 1,158 条 100% 入库），专栏无增量内容；译文面 K3 已全量覆盖，人工精译替换的配对成本与署名体系混杂不值
- **2026-08-15 已删推缺口核销**：pending 2,016 条主体实证是已删转推（RT，1,422 条经 EN 导出实证），本人正文损失极小；留档 `logs/p8_wb_pending.txt` + `~/p8_pending_tweets.md` + `logs/p8_discord.json` + Desktop tappei_tweets
- **2026-08-15 增量收录放弃自动抓取**：fxtwitter 无时间线枚举、CDX 被动滞后、nitter 半死——走手动导出（见上）
- **2026-08-06 英文分类同步到 zh 站**：实测 en 允许的 `[[Category:X]]` 实际主空间仅 45 处且近半是 zh 已删的 en 集名分类或 en 专有分类（Books 等），有效信息量约 20 条书名分类；为 20 条引入自动任务复杂度不值。en 允许的分类列表仍可按需取 logs/en_categories_live.txt
- **2026-07-29 小说:Web 目录页 @media 重构**：ENwiki 目录链接在折叠内只有章节序号没有内容文字（决定不跟随）；改用 `mobileonly`/`nomobile` 类需要 Fandom 职员改 wiki 的 CSS 分组页面（移动版 CSS 只有 Fandom 职员能改），渠道成本高于收益
- **2026-07-26 起不收漫画家/插画师/编辑条目**：六页存稿被否决（先例：否定）；声优/制作人员条目 2026-08-09 起也已删除（收录范围对齐 en 站）
- **不建 en 站搬运工 Loader**：单次工作量已由 replace.py -log 中转法解决；搬运判定（en 有 zh 无）也几乎用不到（2026-07-28 起启用 transferbot 自动化）
- **不建 cssupdate 任务**：MediaWiki 空间读路径全 405，只能人工定期查
- **人类编辑者用 Excel 公式法辅助编辑**（先例：否决改用 bot）：WIKI 编辑不建议再用该方法
